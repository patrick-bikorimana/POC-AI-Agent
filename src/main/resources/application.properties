spring.application.name=POCAIAgent

#(optionnel)
server.port=8080

# Configuration Azure OpenAI
azure.openai.endpoint=${AZURE_OPENAI_ENDPOINT}
azure.openai.key=${AZURE_OPENAI_KEY}
azure.openai.deployment-name=${AZURE_OPENAI_DEPLOYMENT_NAME}